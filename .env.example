# ── Crawler (GitHub Actions secrets / local .env) ─────────────────────────────
SUPABASE_URL=https://esnugiumktntfmvxkvwa.supabase.co
# service_role key: ONLY for the crawler (server side). Never put it in index.html.
SUPABASE_SERVICE_ROLE_KEY=
# SEC requires a contact in the User-Agent: "Company Name you@company.com"
SEC_USER_AGENT=Nexus Asia research you@yourdomain.com
# Optional
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DASHBOARD_URL=https://your-dashboard.vercel.app
# NEXUS_DB=sqlite            # force local SQLite (data/nexus.db)

# ── Dashboard (Vercel → Settings → Environment Variables) ────────────────────
# SUPABASE_URL=...
# SUPABASE_ANON_KEY=         # the anon/public key
# GEMINI_API_KEY=            # enables /api/chat
