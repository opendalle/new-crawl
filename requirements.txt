# Runtime (crawler)
requests>=2.31,<3
feedparser>=6.0.10
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11
python-dotenv>=1.0
# Optional: better company/location extraction for news. The crawler works without it.
# spacy>=3.7   (+ python -m spacy download en_core_web_sm)
